<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug builds talk to the Flutter tooling over the local network. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
